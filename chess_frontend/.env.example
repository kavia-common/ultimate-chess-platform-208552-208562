# Base URL for REST API calls.
# Examples:
# - http://localhost:3001
# - https://your-backend-host
#
# If not set, the frontend uses same-origin requests (works if a reverse-proxy routes /api to backend).
REACT_APP_API_BASE_URL=

# Base URL for Socket.IO.
# Examples:
# - http://localhost:3001
# - https://your-backend-host
#
# If not set, the frontend falls back to REACT_APP_API_BASE_URL and then window.location.origin.
REACT_APP_WS_URL=
